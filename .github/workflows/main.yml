Create a complete GitHub Actions workflow that automatically builds the Android APK for this project.

Requirements:
- Build on every push to main and on manual workflow_dispatch.
- Set up Java 17.
- Grant execute permission to gradlew.
- Build the debug APK using Gradle.
- Upload the generated APK as a GitHub Actions Artifact named "app-debug-apk".
- Detect the correct Android module automatically if needed.
- Ensure the workflow works with this existing project structure without requiring manual changes.
